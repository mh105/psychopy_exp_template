
# Contributing

1. Create a feature branch from `main`.
2. Keep tasks in `builder/`, code in `code/`, conditions in `conditions/`.
3. If you add new standard files, extend `code/check_template.py` & CI.
4. Open a Pull Request with a short summary and screenshots if UI changed.
